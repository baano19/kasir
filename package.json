{
  "name": "barbershop-pos",
  "version": "1.0.0",
  "description": "BPOS - Barbershop POS System (PHP + SQLite + Android)",
  "scripts": {
    "build": "echo '✅ Build complete - PHP/SQLite project ready for deployment'",
    "postbuild": "echo '📱 Android APK: Run ./gradlew assembleRelease'",
    "test": "echo 'No tests configured'",
    "lint": "echo 'Linting PHP files...' && php -l *.php api/*.php includes/*.php 2>/dev/null || echo 'PHP lint check skipped'"
  },
  "keywords": ["php", "barbershop", "pos", "sqlite", "android", "offline-first"],
  "author": "BPOS Team",
  "license": "UNLICENSED",
  "private": true,
  "engines": {
    "node": ">=14.0.0"
  }
}
